---
import { HeaderLogoSVG } from "@/assets";
import { UserIcon } from "lucide-react";
import DesktopNavMenu from "../interactive/desktop-nav-menu";
import MobileNavMenu from "../interactive/mobile-nav-menu";
import ButtonLink from "./button-link.astro";

type Props = {
  clientAreaUrl?: string;
};

const { clientAreaUrl } = Astro.props;
---

<header
  class="bg-background fixed top-0 right-0 left-0 z-20 border-b shadow-sm"
>
  <div class="container flex items-center justify-between py-4">
    <a href="/" title="Página Inicial" aria-label="Página Inicial">
      <HeaderLogoSVG height={50} />
    </a>
    <DesktopNavMenu client:visible pathname={Astro.url.pathname} />
    <div>
      <div class="flex items-center gap-6 text-sm max-lg:hidden">
        {
          clientAreaUrl && (
            <ButtonLink
              title="Área de Clientes"
              target="_blank"
              href={clientAreaUrl}
            >
              <UserIcon />
              <span class="max-xl:hidden">Área de Clientes</span>
            </ButtonLink>
          )
        }
      </div>
      <MobileNavMenu
        client:visible
        pathname={Astro.url.pathname}
        clientAreaUrl={clientAreaUrl}
      />
    </div>
  </div>
</header>
