---
type Props = {
  title?: string;
  description?: string;
};

const { title, description } = Astro.props;
---

<div class="mx-auto max-w-3xl space-y-4 text-center">
  <h2 class="text-secondary text-3xl font-bold">{title}</h2>
  <p class="text-lg">
    {description}
  </p>
</div>
