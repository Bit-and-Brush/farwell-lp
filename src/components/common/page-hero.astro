---
type Props = {
  title: string;
  subtitle: string;
};

const { title, subtitle } = Astro.props;
---

<section class="primary-gradient text-background">
  <div class="mx-auto max-w-3xl space-y-6 px-4 pt-20 pb-36 text-center">
    <h1 class="text-grey-950 text-4xl font-bold">{title}</h1>
    <p class="text-lg">
      {subtitle}
    </p>
  </div>
</section>
