---
const { pathname } = Astro.url;
const links = {
  "/": "Página Inicial",
  "/about": "Sobre nós",
};
---

{
  Object.entries(links).map(([key, value]) => (
    <li class={pathname === key ? "text-primary" : ""}>
      <a href={key}>{value}</a>
    </li>
  ))
}
